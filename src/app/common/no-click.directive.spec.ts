import { NoClickDirective } from './no-click.directive';

describe('NoClickDirective', () => {
  it('should create an instance', () => {
    const directive = new NoClickDirective();
    expect(directive).toBeTruthy();
  });
});
